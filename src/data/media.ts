// External CDN video URLs — keeps videos OUT of the project bundle / dist folder.
// Upload the 3 source clips to Cloudinary (or any CDN) and paste the .mp4 URLs below.
// Until then, the ambient video component falls back to its poster image.

export const AMBIENT_LOUNGE_VIDEO = ""; // homepage hero ambient (WA0012)
export const FEATURED_VIDEO_1 = "";     // lounge page (WA0013)
export const FEATURED_VIDEO_2 = "";     // events page (WA0014)
export const YOUTUBE_TOUR_ID = "";      // optional YouTube embed id
